---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wg-easy-claim
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
  storageClassName: openebs-hostpath
